# Самой игре и домашнему серверу сторонних библиотек не нужно —
# хватает того, что уже есть в Python.
#
# Одна строчка ниже нужна только серверу в интернете. Дело в том,
# что на бесплатном хостинге папка стирается при каждом перезапуске,
# и аккаунты с друзьями пропали бы. Поэтому там данные кладутся
# в базу, а psycopg — это то, чем Python с базой разговаривает.
#
# Дома этот файл ничего не меняет: если базы нет, сервер просто
# пишет в server_data.json рядом с собой, как и раньше.
psycopg[binary]>=3.1
